struct Packet {
    bytes: [u8; 4],
    sibling: i64,
}

fn main() -> i64 {
    let p: Packet = Packet { bytes: [0, 128, 255, 300], sibling: 17 };
    let copied = p.bytes;
    return copied[0] + copied[2] / 2 + copied[3] / 4 + p.sibling;
}
